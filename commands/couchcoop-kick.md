---
description: Kick a guest from the shared couchcoop session
argument-hint: <name>
---
Run `couchcoop ctl kick $ARGUMENTS` via Bash (add `--port <port>` if a non-default port was used) and confirm briefly.
