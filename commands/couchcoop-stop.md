---
description: Stop sharing this session — disconnects all couchcoop guests
---
Run `couchcoop ctl stop` via Bash (add `--port <port>` if a non-default port was used when attaching). Confirm briefly to the user that sharing stopped.
