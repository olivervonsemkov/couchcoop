---
description: Stop sharing this session — disconnects all copair guests
---
Run `copair ctl stop` via Bash (add `--port <port>` if a non-default port was used when attaching). Confirm briefly to the user that sharing stopped.
