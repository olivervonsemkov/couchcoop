---
description: Kick a guest from the shared copair session
argument-hint: <name>
---
Run `copair ctl kick $ARGUMENTS` via Bash (add `--port <port>` if a non-default port was used) and confirm briefly.
